<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:forms="clr-namespace:InTheHand.Forms;assembly=InTheHand.Forms"
             x:Class="filmstermob.Views.Room.RoomPage">
    
    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Chat" Clicked="ChatOpen_Clicked" />
        <ToolbarItem Text="Settings" Clicked="SettingsOpen_Clicked" />
        <ToolbarItem Text="SignOut" Clicked="SignOut_Clicked" />
    </ContentPage.ToolbarItems>

    <ContentPage.Content>
        <StackLayout>
            <forms:MediaElement 
                HorizontalOptions="Fill" 
                VerticalOptions="Center" 
                HeightRequest="240" 
                AutoPlay="False"
                x:Name="mediaPlayer" 
                AreTransportControlsEnabled="True"
                PropertyChanged="MediaPlayer_PropertyChanged"
                CurrentStateChanged="MediaPlayer_CurrentStateChanged_1"
                BindingContextChanged="MediaPlayer_BindingContextChanged"
                Focused="MediaPlayer_Focused"/>
            
            
        </StackLayout>
    </ContentPage.Content>
</ContentPage>